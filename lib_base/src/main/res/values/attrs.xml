<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="LoadingLayout">
        <!-- 空布局资源id -->
        <attr name="llEmptyResId" format="reference"/>
        <!-- 加载中布局资源id -->
        <attr name="llLoadingResId" format="reference"/>
        <!-- 错误布局资源id -->
        <attr name="llErrorResId" format="reference"/>

        <!-- 空布局图片 -->
        <attr name="llEmptyImage" format="reference"/>
        <!-- 空布局文本 -->
        <attr name="llEmptyText" format="string"/>

        <!-- 错误布局图片 -->
        <attr name="llErrorImage" format="reference"/>
        <!-- 错误布局文本 -->
        <attr name="llErrorText" format="string"/>
        <!-- 错误布局重试按钮文本 -->
        <attr name="llRetryText" format="string"/>

        <!-- 文本颜色 -->
        <attr name="llTextColor" format="color"/>
        <!-- 文本尺寸 -->
        <attr name="llTextSize" format="dimension"/>

        <!-- 按钮文本颜色 -->
        <attr name="llButtonTextColor" format="color"/>
        <!-- 按钮文本尺寸 -->
        <attr name="llButtonTextSize" format="dimension"/>
        <!-- 按钮背景 -->
        <attr name="llButtonBackground" format="reference"/>
    </declare-styleable>

    <declare-styleable name="TitleView">
        <attr name="ttLeftDrawable" format="reference"/>
        <attr name="ttTitleText" format="reference|string"/>
        <attr name="ttTitleSize" format="reference|dimension"/>
        <attr name="ttTitleColor" format="reference|color"/>
        <attr name="ttRightDrawable" format="reference"/>
        <attr name="ttTitleBackgroundColor" format="reference|color"/>
    </declare-styleable>
    <declare-styleable name="FlexibleCheckBox">
        <attr name="fcbTitleTextColor" format="color"/>
        <attr name="fcbTitleTextSize" format="reference|dimension"/>
        <attr name="fcbTitleTextStyle" format="reference|integer"/>
        <attr name="fcbTitleText" format="reference|string"/>
        <attr name="fcbBoxType" >
            <flag name="circle" value="0"/>
            <flag name="rectangle" value="1"/>
        </attr>
        <attr name="fcbChecked" format="boolean"/>
    </declare-styleable>
</resources>